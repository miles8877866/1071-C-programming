#include <stdio.h>

int main(){
	int x, y;
	int m[x][y];
	int pos[] = {'E', 'S', 'W', 'N'};
	char face;
	char ins;//instruction
	
	while(scanf("%d %d", &x, &y)!=EOF){
		for(int i=0;i<y;i++){
			for(int j=0;j<x;j++){
				m[x][y]=0;
			}
		}
		scanf("%d %d %c", &x, &y, &face);
		while(scanf("%d", ins)!=)	
		
	}
}
