#include<stdio.h>

int stat(int n)
{
    scanf("%d",&n);
    if(n==-1)
    {
       return 0;
    }
    else
    {
        printf("%d\n",n);
    }
    return stat(n);

}

int main()
{
    int n;
    printf("Enter a series of scores between 0~100(-1 to exit).");
    stat(n);
}
